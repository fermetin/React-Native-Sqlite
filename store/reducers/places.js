import { Places } from '../../model/DummyData'


const initialState = {
    userPlaces: Places,
}

export const palcesReducer = (state = initialState, actions) => {
    switch(actions.type){
     
    }
    return state
}